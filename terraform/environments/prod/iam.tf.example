# 예시 파일입니다.
# 확장자가 `.tf.example` 이므로 Terraform이 읽지 않습니다.
#
# 목적:
# - IAM Role / Policy / Attachment 를 어떻게 분리해서 두는지 예시를 보여주기 위한 파일
#
# 참고:
# - 지금 예시는 EC2 또는 일반 애플리케이션 역할 기준입니다.
# - EKS IRSA 용 Role 이 필요하면 assume_role_policy 를 OIDC federation 형태로 바꿔야 합니다.

# resource "aws_iam_role" "app_role" {
#   name = "${local.name_prefix}-app-role"
#
#   assume_role_policy = jsonencode({
#     Version = "2012-10-17"
#     Statement = [
#       {
#         Effect = "Allow"
#         Principal = {
#           Service = "ec2.amazonaws.com"
#         }
#         Action = "sts:AssumeRole"
#       }
#     ]
#   })
#
#   tags = local.common_tags
# }
#
# resource "aws_iam_policy" "s3_logs_read_only" {
#   name = "${local.name_prefix}-s3-logs-read-only"
#
#   policy = jsonencode({
#     Version = "2012-10-17"
#     Statement = [
#       {
#         Effect = "Allow"
#         Action = [
#           "s3:ListBucket"
#         ]
#         Resource = [
#           "arn:aws:s3:::s3-an2-airline-logs"
#         ]
#       },
#       {
#         Effect = "Allow"
#         Action = [
#           "s3:GetObject"
#         ]
#         Resource = [
#           "arn:aws:s3:::s3-an2-airline-logs/*"
#         ]
#       }
#     ]
#   })
#
#   tags = local.common_tags
# }
#
# resource "aws_iam_role_policy_attachment" "app_role_s3_logs_read_only" {
#   role       = aws_iam_role.app_role.name
#   policy_arn = aws_iam_policy.s3_logs_read_only.arn
# }
#
# resource "aws_iam_instance_profile" "app_role" {
#   name = "${local.name_prefix}-app-instance-profile"
#   role = aws_iam_role.app_role.name
# }
